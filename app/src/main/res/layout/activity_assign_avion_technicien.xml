<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="#F5F7FA">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="👨‍🔧 Assigner un technicien à un avion"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="30dp"
        android:textColor="#333333" />

    <!-- Section Avion -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Sélectionner un avion:"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"
        android:textColor="#555555" />

    <!-- SIMPLIFIEZ LE SPINNER - Retirez le background personnalisé -->
    <Spinner
        android:id="@+id/spinnerAvions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:minHeight="50dp" />

    <!-- Section Technicien -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Sélectionner un technicien:"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"
        android:textColor="#555555" />

    <!-- SIMPLIFIEZ LE SPINNER - Retirez le background personnalisé -->
    <Spinner
        android:id="@+id/spinnerTechniciens"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="40dp"
        android:minHeight="50dp" />

    <!-- Bouton Assigner -->
    <Button
        android:id="@+id/btnAssign"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:text="✅ Assigner le technicien"
        android:textSize="18sp"
        android:textStyle="bold"
        android:backgroundTint="#9C27B0"
        android:textColor="@android:color/white"
        android:padding="12dp"
        android:layout_marginBottom="20dp" />

    <!-- Section informative -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ℹ️ Cette assignation permettra au technicien de voir l'avion dans son interface et de gérer ses interventions."
        android:textSize="14sp"
        android:textColor="#666666"
        android:padding="12dp"
        android:background="#E1BEE7"
        android:backgroundTint="#E1BEE7" />

</LinearLayout>